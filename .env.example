# Copy to .env.local for local dev, and add the same key in Vercel
# Project Settings > Environment Variables (name it exactly XAI_API_KEY).
# Get your key from https://console.x.ai
XAI_API_KEY=your_xai_key_here
